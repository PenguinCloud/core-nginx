---
- name: Check if port 80 and 443 are listening
  wait_for:
    port: "{{ item }}"
    delay: 5
    timeout: 10
  with_items: "{{ ports }}"
  register: ports_result
  until: ports_result is succeeded
  retries: 3
  delay: 10
